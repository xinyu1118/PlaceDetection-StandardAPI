# PlaceDetection-StandardAPI

Check whether the user is in a certain place periodically using Android standard APIs.
If it happens return notification to apps as well as current time in milliseconds. 
